import React from 'react';
import classes from './Model.css';

const model = (props) => {
    return (
        <div className={classes.Model}>
            Hello
        </div>
    );
}

export default model;