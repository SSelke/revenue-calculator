import React from 'react';
import classes from './Expenses.css';

const expenses = (props) => {
    return (
        <div id="expenses" style={props.style}>
            expenses
        </div>
    );
}

export default expenses;